name: 🚀 Deploy Docker app on push

on:
  push:
    branches:
      - main

jobs:
  docker-deploy:
    name: 🐳 Deploy Docker Container
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📤 Upload project files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 22
          source: "./"
          target: "/home/s2276375-node/htdocs/s2276375n.ncgrp.xyz/w12"
          strip_components: 1

      - name: 🐳 Build & restart Docker container on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 22
          script: |
            set -e

            APP_DIR="/home/s2276375-node/htdocs/s2276375n.ncgrp.xyz/w12"
            CONTAINER_NAME="s2276375w12-app"
            IMAGE_NAME="s2276375w12-app:latest"

            cd "$APP_DIR"

            echo "Current directory:"
            pwd
            echo "Files:"
            ls -la

            # Ensure Dockerfile exists before build
            if [ ! -f Dockerfile ]; then
              echo "ERROR: Dockerfile not found in $APP_DIR"
              exit 1
            fi

            # Stop and remove old container (safe on first run)
            docker stop "$CONTAINER_NAME" || true
            docker rm "$CONTAINER_NAME" || true

            # Build latest image
            docker build -t "$IMAGE_NAME" .

            # Run new container
                        docker run -d \
              --name "$CONTAINER_NAME" \
              --restart unless-stopped \
              -p 3045:3045 \
              -e PORT=3045 \
              -e NODE_ENV=production \
              -e SESSION_SECRET="${{ secrets.SESSION_SECRET }}" \
              -e DB_HOST="${{ secrets.DB_HOST }}" \
              -e DB_USER="${{ secrets.DB_USER }}" \
              -e DB_PASSWORD="${{ secrets.DB_PASSWORD }}" \
              -e DB_NAME="${{ secrets.DB_NAME }}" \
              -e DB_PORT="${{ secrets.DB_PORT }}" \
              "$IMAGE_NAME"
<<<<<<< HEAD
              
=======

            sleep 5

>>>>>>> f27438e (Add environment variables to Docker deployment)
            echo "Docker containers:"
            docker ps -a

            echo "Container logs:"
            docker logs "$CONTAINER_NAME" --tail 100 || true

            echo "Testing app inside server:"
            curl -i http://127.0.0.1:3045/register || true
